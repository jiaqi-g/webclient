The Http Server for user to access boost server from web.

Connect to boost server and render result to user.

Run steps

1. modify "config" file to reflect actual settings of needed paths
2. run "publish.sh" to load necessary libs into local maven repos
3. run "run.sh" to compile/package/start server

author: victor 
